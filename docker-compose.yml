services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: baki
      POSTGRES_USER: baki
      POSTGRES_PASSWORD: baki_dev
    ports:
      - "5433:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
